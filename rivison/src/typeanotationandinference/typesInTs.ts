let drink = "chai";

let cups = Math.random() > 0.5 ? 10 : "5";

// cups = undefined; // Type 'undefined' is not assignable to type 'string | number'.ts(2322)

let chaiFlavour: string = "alychi chai";
chaiFlavour = "lemon chai";

let chaiOrder: number = 8;
let isAvailable: boolean;
